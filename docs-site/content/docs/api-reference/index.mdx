---
title: API Reference
description: Protocol, control API, and SDK contracts.
---

# API Reference

Nexis has three API surfaces:

- **Wire protocol** between game client and data plane
- **Control API** for project/key/token management
- **TypeScript SDK API** for client integration

Use these pages as contract references when integrating game code.

## Typed Envelope Contract

```ts twoslash
type Envelope<T = unknown> = {
  v: 1;
  t: string;
  rid?: string;
  room?: string;
  p?: T;
};

// ---cut-before---
const message: Envelope<{ type: string; data: unknown }> = {
  v: 1,
  t: 'room.message',
  room: 'duel_room:arena-1',
  p: { type: 'player.move', data: { x: 4, y: 8 } },
};
```
